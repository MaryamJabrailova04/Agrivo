apiVersion: v1
kind: Service
metadata:
  name: agrivo-backend
spec:
  type: {{ .Values.service.type }}
  selector:
    app: agrivo-backend
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 5000
