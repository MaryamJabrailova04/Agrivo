apiVersion: v1
kind: Service
metadata:
  name: agrivo-frontend
spec:
  type: {{ .Values.service.type }}
  selector:
    app: agrivo-frontend
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
